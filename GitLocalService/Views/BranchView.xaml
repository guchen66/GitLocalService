<UserControl x:Class="GitLocalService.Views.BranchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="What should be the name of the initial branch?" 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4">
                <StackPanel Margin="10">
                    <RadioButton Content="Let Git decide (master)" 
                                 IsChecked="{Binding UseCustomBranchName, Converter={StaticResource BooleanNegationConverter}}"
                                 GroupName="Branch"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <RadioButton Content="Override:" 
                                     IsChecked="{Binding UseCustomBranchName}"
                                     GroupName="Branch"
                                     FontSize="13"
                                     Foreground="#24292E"
                                     VerticalAlignment="Center" />
                        <TextBox Text="{Binding InitialBranchName}"
                                 IsEnabled="{Binding UseCustomBranchName}"
                                 FontSize="13"
                                 Width="150"
                                 Margin="10,0,0,0"
                                 Padding="5" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>