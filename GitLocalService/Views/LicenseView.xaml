<UserControl x:Class="GitLocalService.Views.LicenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Please read the following License Agreement."
                       FontSize="14"
                       Foreground="#24292E"
                       Margin="0,0,0,10" />

            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4" Height="300">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                    <TextBlock Text="{Binding LicenseText}"
                               FontFamily="Consolas"
                               FontSize="12"
                               Foreground="#3D444D"
                               TextWrapping="Wrap"
                               LineHeight="16" />
                </ScrollViewer>
            </Border>

            <StackPanel Margin="0,10,0,0">
                <RadioButton Content="I accept the agreement"
                             IsChecked="{Binding AcceptLicense}"
                             GroupName="License"
                             Margin="0,0,0,10"
                             FontSize="14"
                             Foreground="#24292E" />
                <RadioButton Content="I do not accept the agreement"
                             IsChecked="{Binding NotAcceptLicense}"
                             GroupName="License"
                             FontSize="14"
                             Foreground="#24292E" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>