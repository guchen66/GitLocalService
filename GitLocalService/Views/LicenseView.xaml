<UserControl
    x:Class="GitLocalService.Views.LicenseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock
                Margin="0,0,0,10"
                FontSize="14"
                Foreground="#24292E"
                Text="Please read the following License Agreement." />

            <Border
                Height="300"
                Background="#FFFFFF"
                BorderBrush="#D1D9E0"
                BorderThickness="1"
                CornerRadius="4">
                <ScrollViewer Margin="10" VerticalScrollBarVisibility="Auto">
                    <TextBlock
                        FontFamily="Consolas"
                        FontSize="12"
                        Foreground="#3D444D"
                        LineHeight="16"
                        Text="{Binding LicenseText}"
                        TextWrapping="Wrap" />
                </ScrollViewer>
            </Border>

            <StackPanel Margin="0,10,0,0">

                <RadioButton
                    Margin="0,0,0,10"
                    Command="{Binding AcceptCommand}"
                    Content="I accept the agreement"
                    FontSize="14"
                    Foreground="#24292E"
                    GroupName="License"
                    IsChecked="{Binding AcceptLicense, Mode=OneWay}" />

                <RadioButton
                    Command="{Binding NotAcceptCommand}"
                    Content="I do not accept the agreement"
                    FontSize="14"
                    Foreground="#24292E"
                    GroupName="License"
                    IsChecked="{Binding NotAcceptLicense, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>