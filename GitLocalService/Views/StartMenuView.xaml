<UserControl x:Class="GitLocalService.Views.StartMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select the Start Menu folder where shortcuts to Git will be placed." 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0"
                         Text="{Binding StartMenuFolder}"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,10,0" />
                
                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseCommand}"
                        Width="100" Height="36" />
            </Grid>
            
            <CheckBox Content="Don't create Start Menu folder" 
                      IsChecked="{Binding CreateStartMenuFolder}"
                      FontSize="13"
                      Foreground="#6E7681" />
        </StackPanel>
    </Grid>
</UserControl>