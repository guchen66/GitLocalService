<UserControl x:Class="GitLocalService.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Which terminal emulator do you want to use?" 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4">
                <StackPanel Margin="10">
                    <RadioButton Content="Use MinTTY (the default terminal of Git Bash)" 
                                 IsChecked="{Binding TerminalOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=MinTTY}"
                                 GroupName="Terminal"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Use Windows' default console window" 
                                 IsChecked="{Binding TerminalOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=WindowsConsole}"
                                 GroupName="Terminal"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>