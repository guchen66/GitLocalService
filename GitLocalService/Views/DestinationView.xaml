<UserControl x:Class="GitLocalService.Views.DestinationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select the destination location for Git." 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0"
                         Text="{Binding InstallPath}"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,10,0" />
                
                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseCommand}"
                        Width="100" Height="36" />
            </Grid>
            
            <Border Background="#F6F8FA" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4" Padding="15">
                <StackPanel>
                    <TextBlock Text="Disk Space:" FontSize="12" Foreground="#6E7681" Margin="0,0,0,5" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Required: 300 MB" FontSize="12" Foreground="#24292E" Margin="0,0,20,0" />
                        <TextBlock Text="{Binding AvailableSpace}" FontSize="12" Foreground="#24292E" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>