<UserControl x:Class="GitLocalService.Views.SshView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Which SSH executable do you want to use?" 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4">
                <StackPanel Margin="10">
                    <RadioButton Content="Use bundled OpenSSH" 
                                 IsChecked="{Binding SshOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=BundledOpenSSH}"
                                 GroupName="SSH"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Use external OpenSSH" 
                                 IsChecked="{Binding SshOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=ExternalOpenSSH}"
                                 GroupName="SSH"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>