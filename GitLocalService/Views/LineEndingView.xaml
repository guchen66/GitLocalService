<UserControl x:Class="GitLocalService.Views.LineEndingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="How should Git handle line endings in text files?" 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4">
                <StackPanel Margin="10">
                    <RadioButton Content="Checkout Windows-style, commit Unix-style line endings" 
                                 IsChecked="{Binding LineEndingOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=WindowsCheckoutUnixCommit}"
                                 GroupName="LineEnding"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Checkout as-is, commit as-is" 
                                 IsChecked="{Binding LineEndingOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=AsIs}"
                                 GroupName="LineEnding"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Checkout Unix-style, commit Unix-style line endings" 
                                 IsChecked="{Binding LineEndingOption, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Unix}"
                                 GroupName="LineEnding"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>