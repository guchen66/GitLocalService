<UserControl x:Class="GitLocalService.Views.PullView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="Choose the default behavior of 'git pull'" 
                       FontSize="14" 
                       Foreground="#24292E"
                       Margin="0,0,0,15" />
            
            <Border Background="#FFFFFF" BorderBrush="#D1D9E0" BorderThickness="1" CornerRadius="4">
                <StackPanel Margin="10">
                    <RadioButton Content="Merge (fast-forward if possible)" 
                                 IsChecked="{Binding PullBehavior, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Merge}"
                                 GroupName="Pull"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Rebase" 
                                 IsChecked="{Binding PullBehavior, Converter={StaticResource StringEqualsConverter}, ConverterParameter=Rebase}"
                                 GroupName="Pull"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                    <RadioButton Content="Fast-forward only" 
                                 IsChecked="{Binding PullBehavior, Converter={StaticResource StringEqualsConverter}, ConverterParameter=FastForwardOnly}"
                                 GroupName="Pull"
                                 FontSize="13"
                                 Foreground="#24292E"
                                 Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>